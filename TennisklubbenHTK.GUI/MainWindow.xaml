<Window x:Class="TennisklubbenHTK.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TennisklubbenHTK.GUI"
        mc:Ignorable="d"
        Title="HTK Tennisklub"
        Height="500" 
        Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0"
                  Grid.RowSpan="3"
                  x:Name="MemberListDataGrid">
        </DataGrid>

        <Button x:Name="CreateNewMemberBtn"
                Grid.Row="5"
                Content="Tilføj nyt medlem"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"                
                Height="30"
                Margin="20"
                Click="CreateNewMemberBtn_Click" />
    </Grid>
</Window>
